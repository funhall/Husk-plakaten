# Codemagic: Kotlin Android — debug APK (schema som docs.codemagic.io/native-android)
# Branch: backup/testet-android-2026-05-10

workflows:
  huskplakaten-android-debug:
    name: Husk Plakaten Android (Kotlin debug APK)
    max_build_duration: 60
    # linux_x2 kræver typisk betalt plan — mac_mini_m2 som i Codemagic native Android-samples
    instance_type: mac_mini_m2
    environment:
      java: 17
    scripts:
      - name: Gradlew executable
        script: chmod +x ./gradlew
      - name: Set Android SDK location
        script: |
          echo "sdk.dir=$ANDROID_SDK_ROOT" > "$CM_BUILD_DIR/local.properties"
      - name: Build debug APK
        script: ./gradlew assembleDebug --stacktrace
    artifacts:
      - app/build/outputs/**/*.apk
